
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>View Movies</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <link rel="stylesheet" href="./css/style.css">
  <style>
    body {
      background-color: #f8f9fa;
    }
    .container {
      margin-top: 40px;
    }
    table img {
      width: 60px;
      height: auto;
      object-fit: cover;
      border-radius: 4px;
    }
    <style>
  a.icon i {
    font-size: 20px;
    margin: 0 10px;
    color: #333;
    transition: color 0.3s ease, transform 0.3s ease;
  }

  a.icon:hover i {
    color: red;
    transform: scale(1.2);
  }
</style>

  </style>
</head>
<body>

<div class="container">
  <h2 class="text-center mb-4">Movie List</h2>

  <table class="table table-bordered table-hover table-striped align-middle">
    <thead class="table-dark">
      <tr>
        <th>ID</th>
        <th>Poster</th>
        <th>Title</th>
        <th>Genre</th>
        <th>Director</th>
        <th>Language</th>
        <th>Country</th>
        <th>Runtime</th>
        <th>Release Date</th>
        <th>created_At</th>
        <th>Updated_At</th>
        <th>Trailer</th>
        <th>Movie</th>
        <th>Edit</th>
        <th>Delete</th>
      </tr>
    </thead>
    <tbody>
      <% if (movies.length > 0) { %>
        <% movies.forEach(movie => { %>
          <tr>
            <td><%= movie.movie_id %></td>
            <td>
              <% if (movie.poster_url) { %>
               <img src="<%=movie.poster_url%>" alt="Poster" width="100">

              <% } else { %>
             <img src="https://via.placeholder.com/60x90" alt="No Image">
              <% } %>

            </td>
            <td><%= movie.title %></td>
            <td><%= movie.genre %></td>
            <td><%= movie.director %></td>
            <td><%= movie.language %></td>
            <td><%= movie.country %></td>
            <td><%= movie.runtime %> min</td>
            <td><%= movie.release_date ? movie.release_date.toISOString().split('T')[0] : 'N/A' %></td>
            <td><%= movie.created_at ? movie.created_at.toISOString().split('T')[0] : 'N/A' %></td>
            <td><%= movie.updated_at ? movie.updated_at.toISOString().split('T')[0] : 'N/A' %></td>
            <td><a href="<%=movie.trailer_url%>" target="_blank">Teaser</a></td>
            <td><a href="<%= movie.movie_url %>" target="_blank"> Watch Movie</a></td>


           <!-- Edit icon -->
         <td><a href="/movies/editmovie/<%= movie.movie_id %>" class="icon">
           <i class="fa-solid fa-user-pen"></i>
            </a></td> 

          <!-- Delete icon -->
        <td> <a href="/movies/deletemovie/<%= movie.movie_id %>" class="icon" onclick="return confirm('Are you sure you want to delete this movie?');">
          <i class="fa-solid fa-trash"></i>
            </a></td>

          </tr>
        <% }) %>
      <% } else { %>
        <tr>
          <td colspan="9" class="text-center">No movies found.</td>
        </tr>
      <% } %>
    </tbody>
  </table>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
