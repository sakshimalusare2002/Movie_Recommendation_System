<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Movie Recommendation System</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" />
<<<<<<< Updated upstream

  <style>
    * {
      box-sizing: border-box;
    }
    body {
      margin: 0;
      font-family: 'Inter', sans-serif;
      background-color: #f4f6f9;
      color: #333;
    }

    .dashboard {
      display: flex;
      height: 100vh;
      overflow: hidden;
    }

    .sidebar {
      width: 240px;
      background-color: #1f1f2e;
      color: #fff;
      padding: 20px;
      display: flex;
      flex-direction: column;
    }

    .sidebar h2 {
      text-align: center;
      color: #fff;
      margin-bottom: 20px;
    }

    .nav-link {
      color: #ddd;
      text-decoration: none;
      margin-bottom: 15px;
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 8px 12px;
      border-radius: 6px;
    }

    .nav-link.active,
    .nav-link:hover {
      background-color: #333950;
      color: #fff;
    }

    .main {
      flex-grow: 1;
      display: flex;
      flex-direction: column;
    }

    .topbar {
      height: 60px;
      background-color: #fff;
      padding: 0 20px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      border-bottom: 1px solid #ddd;
    }

    .search-input {
      border: 1px solid #ccc;
      padding: 8px 12px;
      border-radius: 4px;
      width: 300px;
    }

    .profile-dropdown {
      position: relative;
    }

    .profile-btn {
      border: none;
      background: none;
      font-weight: bold;
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .profile-btn .material-icons {
      font-size: 40px;
      color: #555;
    }

    .profile-content {
      display: none;
      position: absolute;
      right: 0;
      top: 110%;
      background: white;
      box-shadow: 0 0 8px rgba(0, 0, 0, 0.1);
      padding: 10px;
      min-width: 180px;
      z-index: 10;
      border-radius: 6px;
    }

    .profile-dropdown:focus-within .profile-content,
    .profile-dropdown:hover .profile-content {
      display: block;
    }

    .profile-content p {
      margin: 4px 0;
      font-size: 0.9rem;
      color: #333;
    }

    .logout-btn {
      margin-top: 8px;
      padding: 6px 12px;
      background-color: #e74c3c;
      color: white;
      border: none;
      border-radius: 4px;
      font-size: 14px;
      cursor: pointer;
      width: 100%;
    }

    .content-area {
      padding: 20px;
      overflow-y: auto;
      background-color: #f9f9fb;
      flex-grow: 1;
    }

    .movie-grid {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      justify-content: center;
    }

    .movie-card {
      position: relative;
      width: calc(100% / 6 - 20px);
      border-radius: 10px;
      overflow: hidden;
      transition: all 0.3s ease;
    }

    .poster-wrapper {
      position: relative;
      width: 100%;
      height: 300px;
      overflow: hidden;
      border-radius: 10px;
    }

    .movie-poster {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
      transition: 0.3s ease;
    }

    .overlay {
      position: absolute;
      bottom: 0;
      background: rgba(0, 0, 0, 0.7);
      color: white;
      width: 100%;
      padding: 12px;
      transition: 0.3s ease;
      opacity: 0;
      transform: translateY(100%);
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .movie-card:hover .overlay {
      opacity: 1;
      transform: translateY(0);
    }

    .movie-title {
      font-size: 16px;
      font-weight: bold;
      text-align: center;
      margin-bottom: 10px;
    }

    .overlay-buttons {
      display: flex;
      flex-direction: column;
      gap: 8px;
      width: 100%;
    }

    .overlay-buttons .btn {
      padding: 8px 12px;
      font-size: 13px;
      border: none;
      background: #ffffff;
      color: #000;
      border-radius: 5px;
      cursor: pointer;
      text-align: center;
      text-decoration: none;
    }

    .overlay-buttons .btn:hover {
      background: #e2e2e2;
    }

    .overlay-buttons form {
      margin: 0;
      width: 100%;
    }

    .overlay-buttons form button {
      width: 100%;
    }
  </style>
  

</head>
<body>
<div class="dashboard">
  <nav class="sidebar">

 
    <h2>🎮 MyMovies</h2>
    <a href="/watchlistpage/watlistmovies" class="nav-link active"><span class="material-icons">stars</span><h2><%= user.name %>'s Watchlist</h2>
</a>

    <a href="#" class="nav-link"><span class="material-icons">video_library</span> Library</a>
    <a href="#" class="nav-link"><span class="material-icons">favorite</span> Liked</a>
    <a href="#" class="nav-link"><span class="material-icons">history</span> Recently Watched</a>
  </nav>

  <div class="main">
    <header class="topbar">
      <input type="text" class="search-input" placeholder="Search movies by title or genre" />
      <% if (user) { %>
      <div class="profile-dropdown" tabindex="0">
        <button class="profile-btn">
          <span class="material-icons">account_circle</span>
          <%= user.name %>
=======
  <link rel="stylesheet" href="./css/userDashboard.css">
 
</head>
<body>
  <div class="app-container" role="main" aria-label="Movie Recommendation Application">
    <!-- Left navigation -->
    <nav class="sidebar" aria-label="Main navigation">
      <h2>MyMovies</h2>
      <div class="nav-links" role="list">
        <a href="#" class="nav-link active" data-section="recommendations" role="listitem" aria-current="page">
          <span class="material-icons" aria-hidden="true">stars</span> Recommendations
        </a>
        <a href="/watchlistpage/page" class="nav-link" data-section="library" role="listitem">
          <span class="material-icons" aria-hidden="true">video_library</span> WatchList
        </a>
        <a href="#" class="nav-link" data-section="liked" role="listitem">
          <span class="material-icons" aria-hidden="true">favorite</span> Liked
        </a>
        <a href="#" class="nav-link" data-section="recently-watched" role="listitem">
          <span class="material-icons" aria-hidden="true">history</span> Recently Watched
        </a>
      </div>

      
      <div style="margin-top: auto; padding: 16px; border-top: 1px solid #444;">
        <% if (user) { %>
          <p style="margin: 0; font-weight: 600;">👤 <%= user.name %></p>
          <p style="margin: 0; font-size: 0.85rem; color: #aaa;"><%= user.email %></p>
        <% } else { %>
          <p style="margin: 0;">Not Logged In</p>
        <% } %>
      </div>
    </nav>

    <!-- Main area -->
    <div style="display:flex; flex-direction: column; height: 100vh; overflow: hidden;">
      <!-- Search bar -->
      <header class="searchbar" role="search">
        <span class="material-icons" aria-hidden="true">search</span>
        <input type="search" id="searchInput" placeholder="Search movies by title or genre" aria-label="Search movies" autocomplete="off" />
      </header>
      
      <!-- Content in scrollable main -->
      <main class="content-area" tabindex="0" role="region" aria-live="polite" aria-atomic="true">
        <!-- Sections will be populated here dynamically -->
      </main>
    </div>

    <!-- Right side details panel -->
    <aside class="details-panel" aria-label="Movie details panel" tabindex="0" hidden>
      <h2 id="detailTitle">Select a movie</h2>
            
      <div class="buttons">
        <button class="btn" id="btnLike" aria-pressed="false" aria-label="Like this movie">
          <span class="material-icons" aria-hidden="true">favorite_border</span> Like
        </button>
        <button class="btn" id="btnWatch" aria-pressed="false" aria-label="Mark this movie as watched">
          <span class="material-icons" aria-hidden="true">visibility</span> Mark Watched
>>>>>>> Stashed changes
        </button>
      </div>
<<<<<<< Updated upstream
      <% } %>
    </header>

    <main class="content-area">
      <h3>Welcome to your dashboard</h3>
      <p>Your personalized movie recommendations will appear here.</p>

      <div class="movie-grid">
        <% if (movies && movies.length > 0) { %>
          <% movies.forEach((movie) => { %>
            <div class="movie-card">
              <div class="poster-wrapper">
                <img src="<%= movie.poster_url %>" class="movie-poster" alt="Movie Poster">
                <div class="overlay">
                  <h5 class="movie-title"><%= movie.title %></h5>
                  <div class="overlay-buttons">
                    <a href="<%= movie.trailer_url %>" target="_blank" class="btn">Trailer</a>
                    
                    <a href="<%= movie.movie_url || '#' %>" class="btn">Watch Movie</a>

                    <form action="/watchlistpage/addWatchlist" method="POST">
                    <input type="hidden" name="movie_id" value="<%= movie.movie_id %>">
                    <button type="submit" class="btn btn-primary">Add to Watchlist</button>
                    </form>

                  </div>
                </div>
              </div>
            </div>
          <% }); %>
        <% } else { %>
          <p>No movies available</p>
        <% } %>
      </div>
    </main>
  <% if (typeof added !== 'undefined' && added !== null) { %>
  <script>
    <% if (added === 'true') { %>
      alert("✅ Movie added successfully to watchlist!");
    <% } else if (added === 'duplicate') { %>
      alert("⚠️ You already added this movie to your watchlist!");
    <% } %>
  </script>
<% } %>



=======
    </aside>
>>>>>>> Stashed changes
  </div>
</body>
</html>
